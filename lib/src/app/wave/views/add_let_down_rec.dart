import 'package:flutter/material.dart';

class AddLetDownRec extends StatelessWidget {
  const AddLetDownRec({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('AddLetDownRec'),
    );
  }
}
